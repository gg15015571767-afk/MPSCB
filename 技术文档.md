# 技术文档 — 智能客服系统（MPSCB）

> 版本：V1.0 · 依据《项目流程.md》Step 4 技术栈 + Step 1/2 澄清结论产出
> 本文件记录**技术栈选型与对接方式**；架构设计（架构图、模块划分、消息总线、数据模型、目录规范、代码规范）见 Step 5 补充章节。

---

## 1. 技术栈总览

| 层 | 选型 | 版本/说明 |
|----|------|-----------|
| 核心 Agent 框架 | **Nanobot** | 已 clone 于 `../nanobot`（原生多平台 channel + provider 抽象 + MCP 加载） |
| 开发语言 | **Python** | 3.11 |
| 消息路由 | **Flask** | 多平台统一 Webhook 入口 |
| 数据存储 | **SQLite**（工单业务）+ **MEMORY.md**（文件记忆） | 轻量、零运维 |
| 会话缓存 | **Redis**（可选） | 后期扩容，V1.0 不引入 |
| 反向代理 | **Nginx**（线上） | 线上部署，V1.0 不引入 |
| AI 模型 | **DeepSeek API** | 经 nanobot `openai_compat` 后端接入 |
| 协议扩展 | **自定义 MCP Server**（工单服务） | 让 Agent 具备创建/查询工单能力 |
| 部署方式 | **Docker / Docker Compose** | 线上部署，V1.0 本地直跑 |

---

## 2. 核心 Agent 框架：Nanobot

**选型理由**：Nanobot 已原生覆盖本项目全部底层设施，**无需重造轮子**：

- **多平台 channel**（`nanobot/channels/`）：飞书、钉钉、Telegram、微信、QQ、Discord、Slack 等，即「统一消息总线」的底层。
- **Provider 抽象**（`nanobot/providers/`）：统一接入 Anthropic / OpenAI 兼容 / DeepSeek 等多家模型，切换模型只改配置。
- **MCP 工具加载**（`nanobot/agent/tools/mcp.py`、`loader.py`）：原生支持挂载自定义 MCP Server。
- **会话与记忆**（`nanobot/session/`、`nanobot/skills/memory`）：提供会话管理与会话记忆基础。

**本项目用法**：Nanobot 作为**基座**，客服业务逻辑（FAQ 匹配、工单、满意度）以**插件 + MCP 工具**的形式挂载其上，不侵入框架核心。

---

## 3. 开发语言：Python 3.11

- 与 Nanobot 技术栈一致（Nanobot 本身为 Python 项目）。
- 项目依赖管理：`pyproject.toml` + `uv`（或 pip + venv，二选一，实现时定）。

---

## 4. 消息路由：Flask 多平台统一 Webhook

**职责**：Flask 作为各 IM 平台的统一 Webhook 入口，接收平台事件回调 → 做验签/解析 → 抹平成 Nanobot 内部标准消息 → 送入 Agent。

**飞书接入说明**（首期平台）：
- 飞书 channel 在 Nanobot 中**同时支持 webhook 与长连接（`feishu/websocket.py`）**两种模式。
- **本地开发推荐长连接模式**：无需公网域名/回调地址即可在本地跑通，降低联调门槛。
- **线上部署走 webhook 模式**：配事件订阅 + 回调地址，经 Nginx + HTTPS 对外。

> 多平台抹平（Message Protocol Translation）的详细设计见 Step 5。

---

## 5. 数据存储：SQLite + 文件记忆

| 数据 | 存储 | 说明 |
|------|------|------|
| 工单、问题记录、满意度 | **SQLite** | 结构化业务数据，单文件、零运维，符合本地单实例定位 |
| 用户偏好（记忆） | **MEMORY.md**（文件记忆） | 按用户维度记录个性化偏好 |

- 工单表结构（字段、状态机）在 Step 5「数据模型」细化。
- SQLite 通过 SQLAlchemy（ORM 模型）访问，便于后期迁移（如换 PostgreSQL/MySQL）。

---

## 6. AI 模型：DeepSeek API

**对接方式**：DeepSeek 走 Nanobot 的 `openai_compat` 后端（已核实 registry 配置）：

| 项 | 值 |
|----|----|
| Provider 名 | `deepseek` |
| 后端 | `openai_compat` |
| API Base | `https://api.deepseek.com` |
| 环境变量 | `DEEPSEEK_API_KEY` |
| 预置模型（Nanobot registry） | `deepseek-v4-flash`、`deepseek-v4-pro`、`deepseek-v4-flash-vision-exp` |

**模型使用建议**（可在配置中调整）：
- 默认 **`deepseek-v4-flash`**：快、便宜，适合 FAQ 兜底这类常规问答。
- 复杂/多轮推理场景可选 **`deepseek-v4-pro`**。

---

## 7. 协议扩展：自定义 MCP Server（工单服务）

**目的**：让 Agent 具备「创建工单 / 查询工单」能力，而不是在对话里硬编码。

- 实现一个**工单 MCP Server**，暴露 `create_ticket`、`query_ticket`（后续 `update_ticket`）等工具。
- 挂载到 Nanobot 的 MCP 工具加载机制（`agent/tools/mcp.py`），Agent 在对话中按需调用。
- 与 SQLite 工单表对接，是业务逻辑与 Agent 能力的边界。

---

## 8. 会话缓存：Redis（可选，后期扩容）

- **V1.0 不引入**；多实例部署需要共享会话状态时再接入（V1.2）。
- 作用：缓存会话、跨实例共享对话上下文。

---

## 9. 反向代理：Nginx（线上部署）

- **V1.0 不引入**；线上部署时用 Nginx 做反向代理 + HTTPS 终结 + 静态资源。
- 对接飞书 webhook 回调的 TLS 需求。

---

## 10. 部署方式：Docker / Docker Compose

- 目标形态：`docker-compose.yml` 编排 Nanobot 服务（+ 后期 Redis / Nginx）。
- V1.0 阶段本地直跑（`python` 启动），Docker 化在 V1.2「稳定优化」完成。

---

## 11. 架构设计（Step 5 补充）

### 11.1 核心架构洞察（先读这个）

经核实 nanobot 源码，**消息路由、多平台抹平、MCP 加载、provider 接入这些基础设施 nanobot 已经全部提供**，本项目无需重造：

| nanobot 已有能力 | 源码位置 | 对 MPSCB 的意义 |
|------------------|----------|-----------------|
| 消息总线 | `bus/events.py`（`InboundMessage`/`OutboundMessage`）、`bus/queue.py`（`MessageBus`） | 统一消息格式已定义，无需自建 |
| 多平台 channel | `channels/base.py`（`BaseChannel`）+ `channels/feishu/` 等 | 「平台抹平」已由 channel 完成 |
| 网关/运行时 | `gateway/runtime.py`、`gateway/service.py` | 负责跑 channel + agent |
| MCP 工具加载 | `agent/tools/mcp.py`、`config/schema.py`（`MCPServerConfig`） | 工单 MCP Server 有现成挂载点 |
| DeepSeek | `providers/registry.py`（openai_compat 后端） | 配 key 即用 |

> ⚠️ **对 Step 4 的一处修正**：原定「消息路由：Flask 多平台统一 Webhook」在本架构中**不再作为核心组件**——飞书的接入与消息抹平已由 nanobot 的 gateway + feishu channel 承担（webhook 或长连接 `feishu/websocket.py` 均可）。Flask 仅**预留**为 V1.1「FAQ 知识库管理后台」的可选 HTTP API，V1.0 不写。

**因此 MPSCB 的实质工作是四层**：配置 nanobot → 实现客服业务领域逻辑 → 把业务能力暴露为 Agent 可调用的工具/MCP → 用 prompt 定义客服人设。

### 11.2 整体架构图

```
┌─────────────────────────────────────────────────────────────┐
│                     飞书（IM 平台）                          │
│      事件订阅 webhook  /  长连接 websocket（本地开发）          │
└───────────────────────────┬─────────────────────────────────┘
                            │
┌───────────────────────────▼─────────────────────────────────┐
│                  Nanobot 框架（../nanobot，作为依赖）          │
│                                                             │
│   feishu channel ──► MessageBus ──► Agent Loop（DeepSeek）    │
│   （消息抹平）        Inbound/        ──► 工具调用              │
│                      Outbound              │                 │
│                                            ▼                 │
│                              MCP / Tool 动态加载              │
└─────────────────────────────────────────────┬───────────────┘
                                              │ 工具协议（stdio MCP）
┌─────────────────────────────────────────────▼───────────────┐
│                     MPSCB 业务层（本项目）                    │
│                                                             │
│   ┌─ 工具层（Agent 可调用）─────────────────────────────┐    │
│   │  Ticket MCP Server：create_ticket / query_ticket /   │    │
│   │                      update_ticket_status           │    │
│   │  FAQ 匹配工具 · 满意度反馈工具 · 用户偏好工具          │    │
│   └───────────────────────┬────────────────────────────┘    │
│                           │                                  │
│   ┌─ 领域服务层（domain）──▼────────────────────────────┐    │
│   │  faq.py · ticket.py · preference.py · feedback.py  │    │
│   └───────────────────────┬────────────────────────────┘    │
│                           │                                  │
│   ┌─ 数据层 ──────────────▼────────────────────────────┐    │
│   │  SQLite（SQLAlchemy）：工单 / 问题记录 / 满意度      │    │
│   │  MEMORY.md（文件记忆）：用户偏好                      │    │
│   └────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

### 11.3 核心模块划分

| 模块 | 职责 | 关键文件（规划） |
|------|------|------------------|
| **配置层** | nanobot 配置：DeepSeek provider + feishu channel + 工单 MCP server + 客服人设 prompt | `agent/config/` |
| **工具层** | 把业务能力暴露为 Agent 可调用工具 | `mpscb/mcp_server/`、`mpscb/tools/` |
| **领域服务层** | 客服业务逻辑：FAQ 匹配、工单、偏好、反馈 | `mpscb/domain/` |
| **数据层** | SQLAlchemy 模型 + SQLite 引擎 + 文件记忆读写 | `mpscb/domain/models.py`、`mpscb/data/` |
| **Prompt 层** | 客服系统人设 + 工具使用规范（三级 FAQ 策略、转人工时机） | `agent/prompt/`、`agent/AGENTS.md` |

### 11.4 统一消息总线设计（多平台消息抹平）

**结论：直接复用 nanobot 的消息总线，不自己实现。**

- 每个 channel（飞书等）把平台原始事件**抹平**成标准 `InboundMessage`（字段：`channel`、`sender_id`、`chat_id`、`content`、`media`、`metadata`）。
- Agent 回复走 `OutboundMessage`，由 channel 负责投递回原平台。
- **会话隔离**：`InboundMessage.session_key = {channel}:{chat_id}`，多平台会话天然不串。
- **扩展新平台**：新增平台 = 在 nanobot 配置里启用对应 channel（如 `weixin`/`telegram`），业务层**零改动**——这正是「统一消息处理支持后续 channel 添加」的落地方式。

### 11.5 数据模型（工单表结构等）

> 工单、问题记录、满意度存 SQLite（SQLAlchemy ORM）；用户偏好走独立 `PreferenceStore` 抽象（V1.0 文件/SQLite → V1.2 Redis，见 §11.9）。

**存储分层策略（结论：部分 Redis，按数据性质分层）**

| 数据 | 主存 | 进 Redis？ | 理由 |
|------|------|-----------|------|
| 工单 `tickets` | SQLite | ❌ | 持久业务核心、多条件查询、可审计 |
| FAQ `faqs` | SQLite | ⚠️ 只读缓存可选 | 静态参考、关键词匹配 |
| 问题记录 `question_records` | SQLite | ❌ | append-only 无限增长，放内存会撑爆 |
| 满意度 `feedbacks` | SQLite | ❌ | 关系型、需统计报表 |
| 用户偏好 `preferences` | 文件/SQLite → **Redis(V1.2)** | ✅ | 热 key-value，天然 Redis Hash |
| 会话状态 `session` | 内存/文件 → **Redis(V1.2)** | ✅ | 易失、跨实例共享、TTL |

**分层原则**：持久业务数据（工单/记录/反馈/FAQ）永远在 SQLite；Redis 只当「热缓存 + 易失状态 + 键值偏好」的加速层，不做业务主存。

**演进路径**：
- **V1.0（本地单实例）**：SQLite（工单/FAQ/记录/反馈）+ 文件（偏好）+ 内存（会话）
- **V1.2（可部署）**：SQLite 不变 + Redis（偏好 → 换 `RedisPreferenceStore` + 会话缓存）

**tickets（工单）**

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | int PK | 自增主键 |
| `ticket_no` | str, unique | 业务工单号，格式 `TK-YYYYMMDD-XXXX` |
| `title` | str | 工单标题 |
| `description` | str | 问题描述 |
| `user_id` | str, index | 用户标识（= `InboundMessage.sender_id`） |
| `channel` | str | 来源平台（feishu） |
| `status` | str, index | 状态机见下 |
| `session_id` | str | 关联会话（`session_key`） |
| `created_at` / `updated_at` | datetime | 时间戳 |

**工单状态机**：`pending`（待处理）→ `processing`（处理中）→ `closed`（已关闭）；任意态可跳 `waiting_human`（待人工）。

**faqs（FAQ 知识库）**

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | int PK | 自增主键 |
| `keywords` | str | 关键词，逗号分隔（用于精确/包含匹配） |
| `question` | str | 标准问法 |
| `answer` | str | 标准答案 |
| `category` | str | 分类（可选） |

**question_records（问题记录）**

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | int PK | 自增主键 |
| `user_id` / `channel` | str | 用户 / 平台 |
| `question` | str | 用户问题 |
| `resolution` | str | 处理路径：`faq_hit` / `llm_fallback` / `human` |
| `faq_id` | int, nullable | 命中 FAQ 时关联 |
| `created_at` | datetime | 时间戳 |

**feedbacks（满意度反馈）**

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | int PK | 自增主键 |
| `user_id` | str | 用户 |
| `session_id` / `ticket_id` | str/int, nullable | 关联会话或工单 |
| `rating` | str | 初版 `satisfied` / `dissatisfied`（V1.1 升级 1–5） |
| `comment` | str, nullable | 可选备注 |

**用户偏好**：按 `user_id` 维度记录键值偏好（称呼、语言、常用问题类型）。不绑死 MEMORY.md，走独立存储抽象 `PreferenceStore`（V1.0 文件/SQLite → V1.2 Redis），详见 §11.9。

### 11.6 项目目录规范

```
MPSCB/
├── CLAUDE.md                             # AI 协作开发规范
├── agent/                                # nanobot agent 位置
│   ├── AGENTS.md                         # 智能客服人设 + 系统信息
│   ├── prompt/                           # 客服人设 system prompt
│   └── config/                           # nanobot 配置（provider/channel/mcp）
├── 项目流程.md / PRD.md / 技术文档.md     # 项目文档
├── README.md / CHANGELOG.md              # Step 6 补齐
├── pyproject.toml                        # 依赖与打包（声明 nanobot 依赖）
├── src/mpscb/                            # 业务包
│   ├── __init__.py
│   ├── domain/                           # 领域服务层
│   │   ├── models.py                     # SQLAlchemy 模型（Ticket/Faq/...）
│   │   └── faq.py  ticket.py  preference.py  feedback.py
│   ├── mcp_server/                       # 工单 MCP Server（stdio）
│   │   └── server.py                     # 暴露 create/query/update 工具
│   └── tools/                            # 其它 nanobot 工具（FAQ 匹配等）
├── data/                                 # 运行时数据（*.db、MEMORY.md）
└── tests/                                # pytest
    ├── test_faq.py  test_ticket.py  test_mcp_server.py
    └── fixtures/                         # 飞书 webhook JSON 样例（Step 9 用）
```

> nanobot 位于 `../nanobot`，作为外部框架依赖（pip 安装 / `-e` 引用），不复制进本仓库。

### 11.7 代码规范、命名规范

- **命名**：模块/函数/变量 `snake_case`；类 `PascalCase`；常量 `UPPER_SNAKE_CASE`；SQLAlchemy 模型类名单数（`Ticket`），表名复数小写（`tickets`）。
- **类型标注**：所有函数签名带类型注解（Python 3.11，可用 `X | None`）。
- **分层边界**：工具层只调领域服务层，不直接碰 DB 引擎；领域服务层封装 SQLAlchemy 会话。
- **配置外置**：API Key、channel 凭据一律走环境变量 / nanobot 配置，禁止硬编码进代码。
- **测试**：每个模块配套 `pytest`，工单 MCP 工具与消息转换逻辑必须有单测（Step 9 质量守门）。
- **日志**：统一用 `loguru`（与 nanobot 一致），敏感信息（用户标识、token）脱敏。

### 11.8 Agent 注入 nanobot 的方式（结论）

**核心结论：nanobot 的 agent 靠「工作区文件 + 配置文件 + 工具插件」三条通道注入，不靠改框架代码。**

| 通道 | 机制 | 落地位置 |
|------|------|----------|
| ① 人设/行为规范/记忆 | workspace 文件（`SOUL.md` / `AGENTS.md` / `USER.md` / `memory/MEMORY.md`），经 Jinja2 模板 `templates/agent/identity.md` 注入 system prompt | `agent/`（即 workspace） |
| ② 模型/渠道/配置 | `config.json`：`providers`(DeepSeek) + `channels`(飞书) + `agents.workspace` + `tools.mcp_servers` | `agent/config.json` |
| ③ 业务工具 | 工单 → MCP Server（stdio）；FAQ/反馈/偏好 → `nanobot.tools` entry_points | `src/mpscb/mcp_server/`、`src/mpscb/tools/` |
| ④ 启动 | `Nanobot.from_config(config_path, workspace)`（SDK）或 `nanobot gateway`（CLI） | — |

**落地映射：`agent/` 目录 = nanobot workspace**

```
agent/
├── SOUL.md            # 智能客服人设（人格/语气/核心原则）
├── AGENTS.md          # 客服行为规范（三级 FAQ 策略、转人工时机）
├── memory/MEMORY.md   # 长期记忆（V1.0 用户偏好暂存这里，见 §11.5 注）
├── config.json        # nanobot 配置（DeepSeek + 飞书 + 工单 MCP + workspace）
└── skills/            # (可选) 客服技能
```

> 说明：原 `agent/AGENTS.md` 的「人设」拆分到 `SOUL.md`，「行为规范」留在 `AGENTS.md`；「系统信息（运行环境/工具/数据）」**不写进文件**，由 `config.json` + entry_points 工具自动注入。

### 11.9 用户偏好存储抽象（File → Redis 可切换）

**原则**：用户偏好是**结构化键值数据**，不塞进 nanobot 的自由文本 `MEMORY.md`（那留给 nanobot 自身的长期记忆机制）。偏好走独立存储接口，agent 只调工具、工具只调接口、接口背后可换后端——换 Redis 对 agent 零改动。

```
agent 调用 get/set_user_preference 工具
        │
        ▼
   domain/preference.py
        │
   PreferenceStore（接口：get / set / get_all）
        ├── FilePreferenceStore      V1.0 默认（JSON 文件 / SQLite）
        └── RedisPreferenceStore     V1.2 切换
```

```python
# src/mpscb/domain/preference.py
from typing import Protocol

class PreferenceStore(Protocol):
    async def get(self, user_id: str, key: str) -> str | None: ...
    async def set(self, user_id: str, key: str, value: str) -> None: ...
    async def get_all(self, user_id: str) -> dict[str, str]: ...

class FilePreferenceStore:
    """V1.0 默认：JSON 文件或 SQLite，零外部依赖。"""
    ...

class RedisPreferenceStore:
    """V1.2：redis-py 异步客户端，一个用户一个 Hash。"""
    async def get(self, user_id, key):
        return await self.redis.hget(f"mpscb:pref:{user_id}", key)
    async def set(self, user_id, key, value):
        await self.redis.hset(f"mpscb:pref:{user_id}", key, value)
    async def get_all(self, user_id):
        return await self.redis.hgetall(f"mpscb:pref:{user_id}")

def build_store(config) -> PreferenceStore:   # 工厂：按配置选后端
    if config.preference.backend == "redis":
        return RedisPreferenceStore(redis.from_url(config.preference.redis_url))
    return FilePreferenceStore(config.preference.path)
```

**关键点**：

1. **Redis 用 Hash**：key `mpscb:pref:{user_id}`，field = 偏好键（`nickname`/`language`/…），value = 值；`HGETALL` 一次拿全一个用户。
2. **user_id 来源**：工具执行时从会话上下文取 nanobot 的 `InboundMessage.sender_id`，按用户天然隔离。
3. **配置切换（零代码改动）**：

```jsonc
// agent/config.json（V1.2）
"preference": { "backend": "redis", "redis_url": "redis://127.0.0.1:6379/0" }
// V1.0 用 "backend": "file"
```

4. **换后端路径**：给 `PreferenceStore` 加一个实现 + 改一行配置，工具与 agent 一行不动。Redis 上线时与 V1.2 会话缓存一并引入。

### 11.10 并发与扩展策略

**nanobot 并发本质：单进程单 agent + asyncio。**（`AgentsConfig` 只有 `defaults`，无多 agent 列表；gateway 里 `AgentLoop.from_config(...)` 一个 + `asyncio.gather` 一个 event loop。）

**三层并发**：

| 层 | 含义 | 机制 | 版本 |
|----|------|------|------|
| ① 多会话并发 | 一个进程同时服务多个用户会话 | asyncio + MessageBus（`session_key=channel:chat_id` 隔离），LLM `await` 让出 CPU | V1.0 内置 |
| ② 子 agent 并发 | 一个 turn 内并行拆解任务 | `SubagentManager.spawn()` + `max_concurrent_subagents`(默认 4) semaphore | 可选，客服用处有限 |
| ③ 多服务器进程 | 横向扩展多实例 | 每个进程一个 `nanobot gateway`，Nginx 反代分发 | V1.2 |

**多 agent 服务器并发 = 多进程**：nanobot 是「单进程单 agent」，无内置 worker 池 / 单进程多 agent，故「多 agent（不同 bot）」或「多实例（同一 bot 多副本）」都靠**多进程**实现：
- **多实例（同一 bot）**：跑 N 个 gateway 进程（`docker-compose --scale` / systemd / supervisord），Nginx upstream 分发；共享业务数据（SQLite → PostgreSQL）+ 共享会话（见下）。
- **多 agent（不同 bot）**：各自独立 workspace/config（不同 `SOUL.md` + channel），天然隔离。

**多实例会话共享（V1.2+ 待定项）**：
- nanobot 会话持久化 = `JsonlSessionStore`（本地文件，单实例够用）；无内置 Redis 后端、无 `session.backend` 配置开关。
- 选项：① **粘性会话**（Nginx 按 user_id 路由，本地 JSONL 兜底，推荐先上）；② **SDK 手动组装** `SessionManager(store=RedisSessionStore(...))`（实现 `SessionStore` Protocol，一次性装配成本）；③ 等上游提供 Redis 后端。
- 决策：**暂不搬源码**；V1.2 先单实例 + 粘性会话；真到多实例优先 ②。

**明确不做（二期）**：Redis 集群、限流、熔断、分布式会话路由、消息队列削峰。
