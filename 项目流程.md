# Vibe Coding开发项目流程

## Step 1：把想法聊清楚

- 为什么要做这个？智能客服系统可以减少人工成本，帮客服回答一些简单问题。
- 核心功能是什么？支持的功能有FAQ查询（知识库匹配）、工单创建、工单查询、用户偏好、问题记录、问题转人工、收集用户反馈的满意度。
- 谁会用这个工具？智能客服系统是给广大用户使用，支持高并发场景（预期使用nginx代理、redis解决高qps问题）
- 使用环境、场景？用户可通过飞书、钉钉、Telegram、微信、QQ等平台使用（初期可以只使用一两个平台）
- 我希望这个项目[本地运行/可部署/具备扩展性]？基于nanobot架构实现，预期可以部署且具备拓展性，比如统一消息处理支持后续chanel添加，支持MCP工具添加。
- 不做：大型多坐席客服系统、付费、复杂权限、企业级高并发（属于二期扩展）

**【Step 1 澄清结论】已拍板的 4 个决策：**
1. 首个接入平台：**飞书**（nanobot 原生 feishu channel；飞书开放平台建应用 + 事件订阅）
2. FAQ 匹配：**关键词/精确匹配优先 → 未命中由 DeepSeek 结合 FAQ 知识库兜底 → 仍不行转人工**（不引入向量库）
3. 问题转人工：**V1.0 仅预留接口 + 工单标记「待人工」**，人工侧具体处理留二期
4. V1.0 运行边界：**本地单实例先跑通**（nanobot + Flask + SQLite + 飞书），Redis/Nginx 后置，不做企业级高并发

## Step 2：要一份 PRD

输出件：**PRD.md**

- 产品定位：这个产品是干什么的
- 用户角色：用户怎么使用这些功能
- 核心功能列表：有哪些功能，每个功能具体做什么。（每个功能带验收标准）
- 非功能需求（性能、安全、部署）
- 技术要求：需要什么技术支持

## Step 3：明确各阶段项目目标

输出件：**PRD.md**的补充

- V1.0 MVP 最小可用版本目标
- V1.1 功能完善版目标
- V1.2 稳定优化版目标

## Step 4：确定技术栈

输出件：**技术文档.md**

- 核心 Agent 框架：Nanobot
- 开发语言：Python 3.11
- 消息路由：Flask 多平台统一 Webhook
- 数据存储：SQLite（工单业务）+ 文件记忆 MEMORY.md
- 会话缓存：Redis（可选，用于后期扩容）
- 反向代理：Nginx（线上部署）
- AI 模型：DeepSeek API
- 部署方式：Docker / Docker Compose
- 协议扩展：自定义 MCP Server（工单服务）

## Step 5：要一份架构设计

输出件：**技术文档.md**的补充

Prompt：根据这个**PRD**和**技术文档**，请为我设计一个合适个人项目的轻量架构，包括：

- 整体架构图
- 核心模块划分
- 统一消息总线设计（多平台消息抹平）
- 数据模型（工单表结构）
- 项目目录规范
- 代码规范、命名规范

## Step 6：整理项目文档

必须四份文档

- PRD.md 产品需求
- 技术文档.md 架构 + 技术栈
- README.md 部署使用说明
- CLAUDE.md 定义 AI 协作规范
- CHANGELOG.md 迭代日志

## Step 7：Git 版本管理

## Step 8：任务拆解与 Prompt 逐模块生成 (Task Breakdown & Vibe Coding) 

- **基础设施**：定义统一数据模型（SQLAlchemy 模型 / Pydantic schema）和接口基类（Unified Message Protocol）。
- **核心 Agent 层**：基于 nanobot 绑定 DeepSeek API，实现最基础的问答（FAQ & 记忆管理）
- **MCP 扩展层**：开发工单 MCP Server，让 Agent 具备创建/查询工单的能力。
- **统一消息路由（Webhook）**：实现 Flask 基础框架与 1 个平台（如 Telegram 或 飞书）的适配接入。
- **平台抹平（Message Protocol Translation）**：将 Incoming Payload 统一转化为内部标准 Message 格式送入 nanobot。
- **E2E 串联与测试**。

## Step 9：自动化测试与质量守门 (Pytest & Code Review with AI)

在 Vibe Coding 中，单元测试是保障 AI 不会“改东墙补西墙”的唯一防线：

- **测试驱动/同步补齐**：要求 AI 每写完一个模块，必须配套输出对应的 `pytest` 测试用例（特别针对工单 MCP 工具和消息转换器）。
- **Mock 外部接口**：准备 Telegram/飞书 Webhook 的真实 JSON Payload 样例作为 Test Case，确保无需每次都部署线上测试。