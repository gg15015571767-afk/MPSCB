# PRD — 智能客服系统（MPSCB）

> 版本：V1.0 草案 · 依据《项目流程.md》Step 1 澄清结论产出
> 上游输入：`项目流程.md` Step 1（六问 + 4 个已拍板决策）

---

## 1. 产品定位

一个基于 **Nanobot** 架构、通过 **飞书**（首期）等 IM 平台接入的**智能客服机器人**。它替人工客服回答简单、重复性问题，降低人工成本；当无法自动解决时，沉淀为工单转人工处理。

**首期边界（V1.0）**：本地单实例运行，只接飞书一个平台，不引入 Redis/Nginx，不做企业级高并发。

**核心价值一句话**：用户遇到常见问题先问机器人，机器人优先用知识库标准答案秒回，答不了的交给 DeepSeek 兜底，再答不了的生成工单转人工，最后回收满意度反馈形成闭环。

---

## 2. 用户角色

| 角色 | 说明 | 使用方式 | 首期范围 |
|------|------|----------|----------|
| 终端用户 | 广大 C 端用户 | 在飞书里与机器人对话：提问、创建/查询工单、反馈满意度 | ✅ 核心 |
| 知识库维护者（客服/运营） | 维护 FAQ 知识库内容 | 编辑 FAQ 条目（初始为静态文件/表，管理界面二期） | ✅ 间接（人工维护内容） |
| 人工客服 | 处理「待人工」工单 | 线下查看 DB 中的待人工工单（坐席系统二期） | ⚠️ 预留接口 |

> 注：首期没有「大型多坐席客服系统」，人工客服通过**线下方式**处理工单，V1.0 仅预留接口与标记。

---

## 3. 核心功能列表（7 项，每项含验收标准）

### F1. FAQ 查询（知识库匹配）

**描述**：用户提问后，系统按「关键词/精确匹配 → LLM 兜底 → 转人工」三级策略应答：
1. 精确/关键词匹配 FAQ 知识库 → 命中则返回**标准答案**；
2. 未命中 → 交给 DeepSeek **结合 FAQ 知识库内容**生成回答（只基于知识库，不编造）；
3. 仍无法解决 → 触发 F6 转人工。

**验收标准**：
- [ ] 用户问题与 FAQ 关键词**精确/包含匹配**时，返回对应标准答案（不调 LLM）。
- [ ] 未命中关键词时，调用 DeepSeek，且回答**仅引用知识库已有内容**，不凭空编造。
- [ ] 知识库确实无法回答时，给出「已为您转人工」提示并进入转人工流程（不硬答）。
- [ ] FAQ 知识库可被维护者**增删改**（首期静态文件/SQLite，管理界面二期）。

---

### F2. 工单创建

**描述**：当用户主动要求「帮我开个工单」、或转人工被触发时，系统创建一条工单并落库，返回**工单号**供后续查询。

**工单字段（初版）**：工单号、标题、问题描述、用户标识、来源平台（飞书）、创建时间、状态、关联会话 ID。

**验收标准**：
- [ ] 用户明确要求创建工单时，系统生成唯一工单号并返回给用户。
- [ ] 工单完整落库（SQLite），字段齐全、可被查询。
- [ ] 转人工触发的工单自动带「待人工」状态（见 F6）。
- [ ] 同一会话内重复创建工单可区分（工单号唯一）。

---

### F3. 工单查询

**描述**：用户凭工单号查询工单当前状态（如：待处理 / 处理中 / 待人工 / 已关闭）。

**验收标准**：
- [ ] 输入**有效工单号** → 返回该工单当前状态与简要信息。
- [ ] 输入**无效/不存在工单号** → 友好提示「未找到该工单，请核对工单号」，不报系统错误。

---

### F4. 用户偏好（记忆）

**描述**：记录用户的个性化偏好（如称呼、语言、常用问题类型），在后续对话中引用，提升体验。存储于文件记忆（`MEMORY.md`）。

**验收标准**：
- [ ] 用户表达偏好（如「以后叫我小王」「用英文回我」）后，偏好被持久化记录。
- [ ] 后续对话中系统能正确引用该偏好（如使用约定称呼/语言）。
- [ ] 偏好按用户维度隔离，不串号。

---

### F5. 问题记录

**描述**：记录每次用户提问及处理结果（命中 FAQ / LLM 兜底 / 转人工），用于后续分析与审计。

**验收标准**：
- [ ] 每次用户提问均留下一条记录，含：用户、问题、处理路径、时间戳。
- [ ] 记录可追溯（能还原「这个用户问了什么、系统怎么答的」）。

---

### F6. 问题转人工

**描述**：当 FAQ 与 LLM 均无法解决、或用户**主动要求转人工**时，将工单标记为「待人工」并告知用户。**V1.0 只预留接口 + 打标记**，人工侧具体处理留二期。

**验收标准**：
- [ ] 触发条件（兜底失败 / 用户明确说「转人工」）命中时，工单状态置为「待人工」。
- [ ] 用户收到「已转人工，请稍候」类明确提示。
- [ ] 暴露**人工侧回调/处理接口的占位**（二期实现，V1.0 接口存在、可被调用但处理逻辑为线下）。

---

### F7. 收集满意度反馈

**描述**：在问题解决（FAQ 命中 / LLM 兜底成功）或工单关闭时，向用户收集满意度（初版：满意 / 不满意 二档，可扩展 1–5 星）。

**验收标准**：
- [ ] 会话/工单结束时，系统**主动请求**用户反馈满意度。
- [ ] 反馈结果落库，并**关联到对应工单/会话**。
- [ ] 用户不回应时**不阻塞**主流程（可跳过，不影响后续对话）。

---

## 4. 非功能需求

| 维度 | 需求 |
|------|------|
| 性能 | 本地单实例即可；FAQ 命中响应 < 1s，LLM 兜底响应可接受数秒；首期不追求高并发。 |
| 安全 | 飞书事件回调**验签**；API Key（DeepSeek / 飞书）走环境变量，不进代码库；敏感信息不落日志。 |
| 部署 | 本地可直接运行；预留 Docker / Docker Compose 打包；Redis、Nginx 后置到扩容期。 |
| 可维护性 | 模块化分层；平台 channel 可插拔（后续加微信/QQ/钉钉）；MCP 工具可扩展。 |
| 可扩展性 | 统一消息总线抹平多平台差异；新增 channel 无需改动核心业务逻辑。 |

---

## 5. 技术要求（细节在 Step 4 技术文档定稿）

| 项 | 选型 |
|----|------|
| 核心 Agent 框架 | Nanobot（已 clone 于 `../nanobot`，原生含飞书 channel、MCP 挂载、DeepSeek provider） |
| 开发语言 | Python 3.11 |
| 消息路由 | Flask 多平台统一 Webhook |
| 数据存储 | SQLite（工单业务）+ 文件记忆 `MEMORY.md`（用户偏好） |
| AI 模型 | DeepSeek API（经 nanobot OpenAI 兼容 provider 接入） |
| 协议扩展 | 自定义 MCP Server（工单服务） |
| 首期平台 | 飞书（feishu channel） |
| 部署 | Docker / Docker Compose（线上） |
| 反向代理 / 缓存 | Nginx、Redis（扩容期，V1.0 不引入） |

---

## 6. 范围外（不做）

- 大型多坐席客服系统（坐席分派、抢单、会话分配）
- 付费 / 计费体系
- 复杂权限（多角色 RBAC）
- 企业级高并发（Redis 集群、限流、熔断）
- 多平台全量接入（微信/QQ/钉钉/Telegram 等留到后续版本）

---

## 7. 分阶段目标（Step 3 补充）

> 三档迭代：V1.0 先把闭环跑通，V1.1 补功能与多平台，V1.2 做部署与稳定。企业级高并发、多坐席、付费、复杂权限始终在范围外（二期）。

### V1.0 — MVP 最小可用

**目标**：本地单实例跑通，只接飞书，7 项功能形成完整闭环。

- 本地单实例运行（Nanobot + Flask + SQLite + 飞书）
- 飞书接入：用户在飞书里与机器人正常对话
- FAQ 查询三级策略可用（关键词匹配 → DeepSeek 兜底 → 转人工）
- 工单创建 / 工单查询（SQLite 落库，工单号唯一）
- 用户偏好记忆（`MEMORY.md`）、问题记录、满意度反馈（满意/不满意二档）
- 转人工：预留接口 + 工单标「待人工」
- 初版 FAQ 知识库可维护（静态文件 / SQLite）

**完成定义（DoD）**：
- [ ] 飞书发一条 FAQ 问题 → 命中并返回标准答案
- [ ] 发一条知识库外问题 → LLM 兜底作答，或转人工
- [ ] 转人工后工单标「待人工」，可用工单号查到
- [ ] 满意度能被收集并落库
- [ ] 每个模块配套 `pytest` 用例（呼应 Step 9 质量守门）

### V1.1 — 功能完善版

**目标**：验证扩展性，补齐运营侧能力。

- 新增**第二个平台**（微信 / 钉钉 / Telegram / QQ 选一），验证统一消息总线可插拔
- FAQ 知识库**管理能力**（增删改的 Web 界面或 CLI）
- 工单**人工侧闭环**：工单状态流转与管理（单坐席级别，非大型坐席系统）
- 满意度从二档升级为 **1–5 星**
- 工单 MCP Server 完善（创建 / 查询 / 状态更新 工具化，供 Agent 调用）
- 用户偏好记忆更丰富（多维度偏好）

### V1.2 — 稳定优化版

**目标**：可部署、可运维、可稳定运行（非企业级高并发）。

- Docker / Docker Compose **一键部署**
- Redis 会话缓存（多实例共享会话）
- Nginx 反向代理 + HTTPS
- 日志与监控、异常告警
- 飞书事件验签等安全加固完善
- 针对「可部署」规模的性能与稳定性优化

> 注：以上 V1.2 的 Redis/Nginx 属于「可部署 + 扩容」级别，**不等于**企业级高并发（Redis 集群、限流、熔断、分布式），后者仍属二期扩展。
