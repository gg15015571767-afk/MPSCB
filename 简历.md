# 简历

## 项目经历

**智能客服系统（MPSCB）** · 个人项目 · Python / Nanobot / DeepSeek / RAG

- **架构与业务闭环**：基于 Nanobot Agent 框架设计「FAQ 匹配 → 向量 RAG → LLM 语义生成 → 自动转人工」的客服流程；通过自定义 MCP Server 实现工单流转，集成飞书与钉钉长连接网关实现双平台实时接入。
- **数据构建与量化评估**：采集清洗 9.1 万条带有 Ground-Truth（最佳回答）的真实电信问答库；编写自动化评估脚本（`eval_retrieval.py`），实测 Top-1 检索平均相似度达 0.864（其中 >0.7 占比 98.7%），实现检索效果的可量化与可复现。
- **模型调优与闭环迭代**：选用 `bge-small-zh` 中文向量模型，通过语义阈值微调（0.45 → 0.55）有效消除了业务词误匹配问题；开发未命中问题挖掘机制（`suggest_faq`），实现高频新问题的知识库自更新闭环。
- **工程化交付**：项目完成 57 个 pytest 单元测试，规范化依赖版本（对齐 PyPI `nanobot-ai==0.3.0`），并通过 Docker Compose 实现容器化部署。

技术栈：Python · Nanobot · DeepSeek · sentence-transformers(bge-small-zh) · SQLite/SQLAlchemy · Docker · MCP

---

## AI 应用技能

**智能客服系统（MPSCB）**

**项目目标与背景**：独立完成一个「能自动回答、效果可量化」的电信智能客服，借此验证并沉淀「用 AI 协作高效交付完整应用」的能力。

**AI 工具及模型选择与原因**：
- 开发协作：**Claude Code**（代码生成、架构设计、框架源码调研、debug、文档）
- 生成模型：**DeepSeek**（中文能力强、成本低、OpenAI 兼容）
- 向量模型：**bge-small-zh**（中文 embedding、本地部署、无需额外 API）
- 选择原则：聚焦业务逻辑、不重造轮子、成本可控、结果可复现

**我与 AI 的分工**：
- 我负责：定方向（领域/数据/架构选型）、技术取舍拍板、代码 Review 与验收、真实环境测试
- AI 负责：代码实现、测试编写、源码调研、调试、文档生成
- 协作模式：**「我判断决策、AI 执行生成、我逐模块验收」**，每个模块跑通测试后才进入下一步

**实现流程**：想法澄清 → PRD → 架构设计 → 技术栈 → 逐模块实现（每模块配 pytest）→ 测试与部署，严格按 9 步流程推进。

**核心挑战与解决方案**：
- 数据无 ground-truth 无法评估 → 换带「最佳回答」的真实问答，建立量化评估脚本
- 中文场景用英文向量模型效果差 → 换 `bge-small-zh`
- 语义阈值导致业务词误匹配 → 实测调优（0.45 → 0.55）
- 框架版本漂移（本地领先 PyPI 775 commit）→ 对齐稳定版 `nanobot-ai==0.3.0`

**项目结果**：检索平均相似度 **0.864**（>0.7 占 98.7%）、**57 个**单测、飞书+钉钉双平台、Docker 部署，形成完整的「RAG + 客服闭环 + 可观测」。
