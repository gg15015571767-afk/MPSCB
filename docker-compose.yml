services:
  mpscb:
    build:
      context: ..
      dockerfile: MPSCB/Dockerfile
    env_file:
      - .env
    volumes:
      - mpscb-data:/app/mpscb/data
      - mpscb-sessions:/app/mpscb/sessions
    restart: unless-stopped

volumes:
  mpscb-data:
  mpscb-sessions:
