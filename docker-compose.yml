services:
  mpscb:
    build: .
    env_file:
      - .env
    volumes:
      - mpscb-data:/app/data
      - mpscb-sessions:/app/sessions
    restart: unless-stopped

volumes:
  mpscb-data:
  mpscb-sessions:
