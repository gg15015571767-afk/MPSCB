# 面试.md — MPSCB 项目面试准备

> 安徽电信智能客服 · 核心卖点：**数据自研 + 准确度真测 + RAG + 完整客服闭环**

---

## 1. 一句话定位（30 秒电梯陈述）

> 我做了一个基于 Nanobot 框架的**安徽电信智能客服**，最大特点是**数据自己找、准确度真测过**——9.1 万条真实问答带标准答案（ground-truth），检索质量实测 0.864；核心是 **RAG**（FAQ 语义检索 + 向量检索问答库 + 生成回答），配上完整的客服业务闭环（工单、转人工、满意度、反馈闭环）和运维观测。

---

## 2. 核心差异点（开场就强调，这是区分别人的地方）

**别人做 RAG 项目通常**：
- 用现成 benchmark（SQuAD 等）或干脆不评估，只展示「能跑通」；
- 数据是下载的现成集，没有「标准答案」可对照。

**我做的不一样**：
- **自己找到真实业务数据** —— 9.1 万条电信「提问 + 最佳回答」，自带 ground-truth；
- **真正评估了回答准确度** —— 检索平均相似度 0.864、>0.7 占 98.7%，端到端 5/5 召回相关回答；
- 有评估脚本（`eval_retrieval.py`），**可复现、可量化**，不是「我觉得它准」。

> 面试话术：「我特别看重『怎么证明它准』，所以花时间找了带标准答案的真实数据，写了评估脚本，跑出了检索质量 0.864。这个在大多数 demo 项目里是没有的。」

---

## 3. 完整介绍（2–3 分钟，STAR 结构）

**背景（S）**：电信客服有大量重复问题（宽带测速、话费查询、流量套餐、积分兑换），人工成本高。

**方案（T）**：做一个「自动回答 → 答不上转人工 → 效果可观测」的智能客服，而不是简单的关键词机器人。

**行动（A，讲架构）**：
- 框架 Nanobot（agent 基座）+ DeepSeek（生成）+ sentence-transformers（embedding）+ SQLite（业务数据）+ MCP（工单工具）。
- 核心是 **RAG 三段式**：`语义匹配 FAQ → 向量检索 9 万条问答 → 生成回答`，仍答不上就转人工。
- 双平台（飞书 + 钉钉），Docker 部署，57 个单测。

**结果（R，量化）**：检索平均相似度 0.864；命中率/满意度/工单可观测；知识库能根据高频未命中问题自更新。

---

## 4. 技术栈（快速报一遍）

| 层 | 选型 |
|----|------|
| Agent 框架 | Nanobot（`nanobot-ai==0.3.0`，PyPI 稳定版） |
| 语言 | Python 3.11 |
| 生成模型 | DeepSeek（chat） |
| RAG embedding | sentence-transformers + `bge-small-zh-v1.5`（中文向量） |
| 数据存储 | SQLite（工单/FAQ/记录/反馈/偏好） |
| 协议扩展 | 自定义 MCP Server（工单服务） |
| 接入平台 | 飞书 + 钉钉（长连接） |
| 部署 | Docker / Docker Compose |

---

## 5. 量化数据（背下来）

- **9.1 万条**真实电信问答（带「最佳回答」ground-truth）
- 检索平均相似度 **0.864**，>0.7 占 **98.7%**
- **57 个** pytest 单测
- 双平台（飞书 + 钉钉）、Docker 部署、MCP 工单服务

---

## 6. 高频面试问题 + 答题思路

1. **为什么用 RAG 而不是微调？**
   → 成本低、知识可实时更新（改了 FAQ 立即生效）、可解释（能追溯检索到的原文）、不依赖训练数据。

2. **embedding 模型怎么选？**
   → 中文场景选 `bge-small-zh-v1.5`（BAAI 中文向量模型）；一开始用过英文的 `all-MiniLM`，发现中文效果差，换成中文模型——**模型要和语言匹配**。

3. **检索质量怎么评估？**
   → 用带 ground-truth 的问答数据，抽测试集检索 top-1，算平均相似度 + 高分占比；另做端到端人工核对。

4. **语义阈值怎么定？**
   → 阈值是超参数，实测调优：0.45 会误匹配（「翼支付白条」错配到「欠费停机」），调到 0.55 排除误匹配、保留正确命中。

5. **为什么用 Nanobot 框架？**
   → 它原生提供多平台 channel、消息总线、MCP 加载、provider 抽象，我只需聚焦客服业务逻辑，不重造轮子。

6. **「答不上」怎么处理？**
   → 三级兜底：FAQ → RAG 检索 → 转人工生成工单，绝不硬编造。

7. **知识库怎么持续更新？**
   → 反馈闭环：分析高频未命中问题（`suggest_faq`），建议补进 FAQ。

8. **数据从哪来？**
   → 公开的电信「知道」类问答数据集（提问 + 网友回答 + 最佳回答），我过滤出「最佳回答」作为标准答案。

---

## 7. 踩坑与反思（加分项，展示「发现问题→定位→解决」）

| 踩的坑 | 学到的 |
|--------|--------|
| 本地 nanobot 领先 PyPI 775 个 commit，代码用了新 API | **框架版本以发布版为准**，对齐 `nanobot-ai==0.3.0` |
| 一开始用英文评论数据，发现「评论不是问答、没法评估准确率」 | **评估需要 ground-truth**，换成真实「提问+最佳回答」数据 |
| 中文场景用英文 embedding 模型 | **模型要和语言匹配**，换 `bge-small-zh` |
| 语义阈值 0.45 导致误匹配 | **阈值是超参，要实测调优** |

---

## 8. 代码地图（被问「代码在哪」能秒答）

| 能力 | 位置 |
|------|------|
| RAG 语义匹配 + embedding | `src/mpscb/domain/rag.py` |
| 问答检索 + 数据加载 | `src/mpscb/domain/review_rag.py` |
| 业务服务（工单/FAQ/反馈/偏好/统计） | `src/mpscb/domain/` |
| 7 个 nanobot 工具（含 `search_qa`） | `src/mpscb/tools/` |
| 工单 MCP Server | `src/mpscb/mcp_server/` |
| 检索质量评估 | `scripts/eval_retrieval.py` |
| 运营报表 / 反馈闭环 | `scripts/stats_cli.py` / `scripts/suggest_faq.py` |
| 启动入口 | `scripts/run_bot.py` |

---

## 附：自我介绍主线（一句话版）

> **「数据自研 + 准确度真测」打头 → 「RAG 向量检索」+「业务闭环」支撑 → 「踩坑反思」收尾。**
> 四段下来，面试官记住的是：一个「用数据说话、真做过完整项目」的人，而不是「跑了个 demo」。
